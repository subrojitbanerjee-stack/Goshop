<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>goshop | Fresh Finds, Best Prices</title>
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        :root {
            --primary: #2563eb;
            --dark: #1e293b;
            --light: #f8fafc;
            --white: #ffffff;
            --accent: #10b981;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Inter', sans-serif; scroll-behavior: smooth; }

        body { background-color: var(--light); color: var(--dark); line-height: 1.6; }

        /* Hero Section */
        .hero {
            height: 80vh;
            background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://i.ibb.co/HfyQ1LYr/20260227-232513.jpg');
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: var(--white);
            padding: 20px;
        }

        .hero h1 { font-size: 3.5rem; margin-bottom: 10px; }
        .hero p { font-size: 1.2rem; margin-bottom: 30px; opacity: 0.9; }
        .btn-shop { 
            padding: 15px 40px; background: var(--primary); color: white; 
            text-decoration: none; border-radius: 50px; font-weight: 600; 
            transition: transform 0.3s;
        }
        .btn-shop:hover { transform: scale(1.05); }

        /* Carousel Section */
        .section-title { text-align: center; margin: 50px 0 30px; font-size: 2rem; }
        
        .carousel-container {
            display: flex;
            overflow-x: auto;
            gap: 20px;
            padding: 20px;
            scroll-snap-type: x mandatory;
            scrollbar-width: none;
        }
        .carousel-container::-webkit-scrollbar { display: none; }

        .product-card {
            min-width: 280px;
            background: var(--white);
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
            scroll-snap-align: start;
            flex-shrink: 0;
            transition: 0.3s;
        }

        .product-card img { width: 100%; height: 250px; object-fit: cover; }
        .product-info { padding: 20px; }
        .product-price { color: var(--primary); font-weight: 700; font-size: 1.2rem; margin: 10px 0; }
        .add-to-cart {
            width: 100%; padding: 10px; border: none; background: var(--dark);
            color: white; border-radius: 8px; cursor: pointer; font-weight: 600;
        }

        /* Floating Elements */
        .cart-trigger {
            position: fixed; bottom: 30px; right: 30px;
            background: var(--primary); color: white; width: 60px; height: 60px;
            border-radius: 50%; display: flex; align-items: center; justify-content: center;
            cursor: pointer; box-shadow: 0 4px 20px rgba(0,0,0,0.2); z-index: 100;
        }
        .cart-count {
            position: absolute; top: 0; right: 0; background: red;
            font-size: 0.7rem; padding: 4px 8px; border-radius: 50%;
        }

        .whatsapp-btn {
            position: fixed; bottom: 100px; right: 35px;
            background: #25d366; color: white; width: 50px; height: 50px;
            border-radius: 50%; display: flex; align-items: center; justify-content: center;
            text-decoration: none; font-size: 1.5rem; z-index: 100;
        }

        /* Cart Drawer */
        #cart-drawer {
            position: fixed; top: 0; right: -100%; width: 100%; max-width: 400px;
            height: 100%; background: white; z-index: 200; transition: 0.4s;
            box-shadow: -5px 0 15px rgba(0,0,0,0.1); padding: 30px; overflow-y: auto;
        }
        #cart-drawer.active { right: 0; }
        .close-cart { float: right; cursor: pointer; font-size: 1.5rem; }

        .form-group { margin-bottom: 15px; }
        .form-group input { width: 100%; padding: 12px; border: 1px solid #ddd; border-radius: 5px; }
        .send-btn { width: 100%; padding: 15px; background: var(--accent); border: none; color: white; font-weight: bold; border-radius: 5px; cursor: pointer; }

        @media (max-width: 600px) {
            .hero h1 { font-size: 2.2rem; }
        }
    </style>
</head>
<body>

    <header class="hero">
        <h1>goshop</h1>
        <p>Fresh Finds, Best Prices</p>
        <a href="#products" class="btn-shop">Shop Now</a>
    </header>

    <h2 class="section-title" id="products">Featured Products</h2>
    <div class="carousel-container" id="product-list">
        </div>

    <a href="https://wa.me/99999999" class="whatsapp-btn" target="_blank">
        <i class="fab fa-whatsapp"></i>
    </a>

    <div class="cart-trigger" onclick="toggleCart()">
        <i class="fas fa-shopping-cart"></i>
        <span class="cart-count" id="count">0</span>
    </div>

    <div id="cart-drawer">
        <span class="close-cart" onclick="toggleCart()">&times;</span>
        <h3>Your Cart</h3>
        <hr style="margin: 20px 0;">
        <div id="cart-items" style="margin-bottom: 30px;"></div>
        
        <form id="order-form">
            <div class="form-group">
                <input type="text" id="cust-name" placeholder="Full Name" required>
            </div>
            <div class="form-group">
                <input type="tel" id="cust-phone" placeholder="Phone Number" required>
            </div>
            <button type="submit" class="send-btn">Place Order via Email</button>
        </form>
    </div>

    <script>
        // Product Data
        const products = [
            { id: 1, name: "Premium Item 1", price: 29.99, img: "https://i.ibb.co/bR8R0BvV/IMG-0895.jpg" },
            { id: 2, name: "Modern Collection", price: 45.00, img: "https://i.ibb.co/yFr0P33p/image.jpg" },
            { id: 3, name: "Essential Kit", price: 15.50, img: "https://i.ibb.co/chKzpf73/image.jpg" },
            { id: 4, name: "Urban Choice", price: 89.00, img: "https://i.ibb.co/PG9tSjFw/image.jpg" },
            { id: 5, name: "Global Edition", price: 55.00, img: "https://i.ibb.co/8ngXmC92/515-R4-CLGOHL-AC-UF894-1000-QL80-FMwebp.webp" },
            { id: 6, name: "Classic Blend", price: 34.00, img: "https://i.ibb.co/gMF8wMMH/d9a73e0a7d79e654af7353c1e33744b2.jpg" },
            { id: 7, name: "Select Series", price: 62.00, img: "https://i.ibb.co/MxXNQjxX/35cae80761902d61b4d240ccda344bd3.jpg" }
        ];

        let cart = [];

        // Render Products
        const productList = document.getElementById('product-list');
        products.forEach(p => {
            productList.innerHTML += `
                <div class="product-card">
                    <img src="${p.img}" alt="${p.name}">
                    <div class="product-info">
                        <h3>${p.name}</h3>
                        <p class="product-price">$${p.price}</p>
                        <button class="add-to-cart" onclick="addToCart(${p.id})">Add to Cart</button>
                    </div>
                </div>
            `;
        });

        // Cart Logic
        function toggleCart() {
            document.getElementById('cart-drawer').classList.toggle('active');
        }

        function addToCart(id) {
            const item = products.find(p => p.id === id);
            cart.push(item);
            updateCartUI();
        }

        function updateCartUI() {
            document.getElementById('count').innerText = cart.length;
            const cartList = document.getElementById('cart-items');
            cartList.innerHTML = cart.length === 0 ? '<p>Empty</p>' : '';
            cart.forEach((item, index) => {
                cartList.innerHTML += `<p style="margin-bottom:10px;">${item.name} - $${item.price} <small style="color:red; cursor:pointer;" onclick="removeItem(${index})"> (Remove)</small></p>`;
            });
        }

        function removeItem(index) {
            cart.splice(index, 1);
            updateCartUI();
        }

        // EmailJS Integration
        (function() {
            emailjs.init("YOUR_PUBLIC_KEY"); // Replace with your EmailJS Public Key
        })();

        document.getElementById('order-form').onsubmit = function(e) {
            e.preventDefault();
            
            if(cart.length === 0) return alert("Add items first!");

            const itemsList = cart.map(i => `${i.name} ($${i.price})`).join(", ");
            const templateParams = {
                to_email: 'subrojitbanerjee@gmail.com',
                from_name: document.getElementById('cust-name').value,
                phone: document.getElementById('cust-phone').value,
                items: itemsList
            };

            emailjs.send('YOUR_SERVICE_ID', 'YOUR_TEMPLATE_ID', templateParams)
                .then(() => {
                    alert('Order Sent! Thank you.');
                    cart = [];
                    updateCartUI();
                    toggleCart();
                }, (err) => {
                    alert('Failed to send... Check EmailJS Keys.');
                    console.log(err);
                });
        };
    </script>
</body>
</html>
